[pytest]
asyncio_mode = auto
testpaths = tests
# The legacy *_test.py / test_iteration*.py files are live-server integration
# tests that hit a deployed URL; the hermetic unit suite lives in test_*_unit.py
# and the security suite. Run the hermetic suite by default.
python_files = test_security.py test_*_unit.py
